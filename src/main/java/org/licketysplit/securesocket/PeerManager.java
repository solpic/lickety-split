package org.licketysplit.securesocket;

public class PeerManager {
    public PeerManager() {}
    public void addPeer(PeerInfo peer) {

    }
    public static PeerManager initialize(PeerInfo initialPeer) throws Exception {
        PeerManager pm = new PeerManager();
        return pm;
    }
}
